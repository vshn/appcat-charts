suite: Admin Secret template (empty username)
release:
  name: forgejo-unittests
  namespace: testing
templates:
  - templates/gitea/admin-secret.yaml
tests:
  - it: renders nothing
    set:
      gitea.admin.username: ''
    asserts:
      - hasDocuments:
          count: 0
