suite: Admin Secret template (existing secret)
release:
  name: forgejo-unittests
  namespace: testing
templates:
  - templates/gitea/admin-secret.yaml
tests:
  - it: renders nothing
    set:
      gitea.admin.existingSecret: 'some-secret'
    asserts:
      - hasDocuments:
          count: 0
