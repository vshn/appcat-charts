suite: Init template (admin password empty)
release:
  name: forgejo-unittests
  namespace: testing
templates:
  - templates/gitea/init.yaml
tests:
  - it: contains configure_admin_user section
    asserts:
      - matchRegex:
          path: stringData["configure_gitea.sh"]
          pattern: configure_admin_user
